import { CLEAR_MISSION ,CREAT_MISSION,READ_MISSION,ADD_MISSION} from '../action/missions';

sexport default function missionsReducer(state = initialState, action) {
	case ADD_MISSION:
    return state;
  default:
      return state;
  }
}
